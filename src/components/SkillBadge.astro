---
interface Props {
    skill: string;
}

const { skill } = Astro.props;
---

<button
    class="px-4 py-2 bg-neutral-100 hover:bg-neutral-200 text-gray-900 rounded-full text-sm font-medium transition"
>
    {skill}
</button>
